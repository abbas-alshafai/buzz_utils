name: buzz_utils
description: String, lists, and maps utilities
version: 0.0.3
homepage: https://github.com/abbas-alshafai/buzz_utils

environment:
  sdk: ">=2.16.1 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  sprintf: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
